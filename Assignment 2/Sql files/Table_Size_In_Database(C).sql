SELECT blocks - empty_blocks
FROM user_tables
WHERE table_name = 'DISTRIBUTEDBY';

SELECT blocks - empty_blocks
FROM user_tables
WHERE table_name = 'MOVIE';

SELECT blocks - empty_blocks
FROM user_tables
WHERE table_name = 'PEOPLE';

SELECT blocks - empty_blocks
FROM user_tables
WHERE table_name = 'PLAYS';

SELECT blocks - empty_blocks
FROM user_tables
WHERE table_name = 'PRODUCEDBY';